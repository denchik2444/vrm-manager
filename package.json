{
  "name": "vroid-manager",
  "version": "1.1.5",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=cjs --external:astra-plugin-sdk --outfile=dist/index.js",
    "check": "node --check dist/index.js"
  },
  "dependencies": {
    "astra-plugin-sdk": "^0.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.10.0"
  },
  "main": "dist/index.js"
}
